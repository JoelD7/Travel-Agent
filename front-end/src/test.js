//Import functions like this to prevent 'SyntaxError: Cannot use import statement outside a module'
const { parseISO } = require("date-fns");

let s = "trippejwttokenrultrasecuresecretkey";
console.log(s.length);
