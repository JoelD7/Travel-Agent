import { makeStyles, Theme } from "@material-ui/core";

export const appStyles = makeStyles((theme: Theme) => ({
  dialogTitle: {
    display: "flex",
  },
  paperWidthSm: {
    width: "515px",
    borderRadius: "10px",
  },
}));
